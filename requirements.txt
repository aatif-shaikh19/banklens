# BankLens 2.0 — Python Dependencies
# Python 3.11 required
# Strategy: conservative pinning, tested-compatible combinations
# pandasai removed — conflicts with pandas 3.x (use openai directly instead)
# Install: pip install --no-cache-dir -r requirements.txt

# ── Core Data Science ─────────────────────────────────────────────────────────
# pandas 3.x required for xgboost 3.x and scikit-learn 1.9.x compatibility
pandas==3.0.3
numpy==2.2.3
scipy==1.15.2

# ── ML / Explainability ───────────────────────────────────────────────────────
scikit-learn==1.9.0
xgboost==3.2.0
shap==0.47.2
joblib==1.4.2

# ── API Layer ─────────────────────────────────────────────────────────────────
fastapi==0.136.3
uvicorn[standard]==0.34.0
pydantic==2.10.6
slowapi==0.1.9

# ── Data Quality ──────────────────────────────────────────────────────────────
# v1.x API is completely different from v0.18.x — do NOT use 0.18.x tutorials
great-expectations==1.15.2

# ── dbt ───────────────────────────────────────────────────────────────────────
# dbt-core 1.9.0: stable, well-tested minor version
# dbt-snowflake 1.11.1: MUST be >=1.10.6 (Snowflake May 2026 column type change)
# dbt adapters can be a higher minor version than dbt-core — this is supported
dbt-core==1.11.6
dbt-bigquery==1.11.1
dbt-snowflake==1.11.1

# ── Cloud Connectors ──────────────────────────────────────────────────────────
# google-cloud-bigquery: do NOT pin protobuf — let pip resolve it
# protobuf is a transitive dependency shared with grpcio; pinning causes conflicts
google-cloud-bigquery==3.25.0
google-cloud-bigquery-storage>=2.33,<3
# snowflake-connector 4.x is very new and has sqlalchemy compat issues
# staying on 3.12.2 which is confirmed stable
snowflake-connector-python==4.0.0
snowflake-sqlalchemy>=1.8,<2.0
pyarrow==17.0.0
fsspec==2024.6.1

# ── File Formats ──────────────────────────────────────────────────────────────
openpyxl==3.1.5

# ── Visualization ─────────────────────────────────────────────────────────────
matplotlib==3.10.1
seaborn==0.13.2
plotly==5.24.1
streamlit==1.58.0
kaleido==0.2.1

# ── AI Layer ──────────────────────────────────────────────────────────────────
# pandasai REMOVED — incompatible with pandas 3.x regardless of version
# We use openai directly — see chat/chat_with_data.py for the replacement
# Snowflake Cortex Analyst (no package needed — built into Snowflake) is
# the primary AI showcase for HSBC/JPM interviews
openai==1.60.2

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.2.2
requests==2.32.3
httpx==0.28.1

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.3.5
pytest-asyncio==0.25.2

# ── Security ──────────────────────────────────────────────────────────────────
pip-audit==2.8.0
bandit==1.8.3
detect-secrets==1.5.0